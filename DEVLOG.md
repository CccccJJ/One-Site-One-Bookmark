# DEVLOG

记录每次功能/修复的背景、决策、改动与验证。**新记录追加在「开发记录」最上方**（时间倒序），每节标注对应提交。

## 当前状态（2026-09-26，`9b370fc`）

- **核心**：按网站开关。某网站开启后，在该网站新建书签会删除同一 hostname 下的其他书签，只留最新一个。
- **保护**：开启前显示本站收藏数，多于 1 个需确认；删除的书签进入「最近删除」（最多 50 条、保留 7 天），可恢复到原文件夹原位置；导入书签期间不处理。
- **最近删除管理**：默认折叠；显示删除时间；可删除单条、两步确认清空。
- **验证方式**：`node --check` + 模拟 `chrome` API 的 node 测试（22 项）+ headless Chrome 截图预览 popup；真实 Chrome 由 cc 手动验证。

## 待办 / 遗留

- [ ] **测试与预览脚本不在仓库里**：模拟测试 `sw_test2.mjs`、popup 预览 `preview/`、截图脚本 `shoot.sh` 都在后台任务的临时目录 `~/.claude/jobs/de96e5bc/tmp/`，任务删除后会一起消失。需要决定是否移入仓库（如 `tests/`，需先在 CLAUDE.md 定目录约定）。
- [ ] **`.claude/settings.json` 未生效**：后台会话（从 `claude agents` 启动）默认只能在 worktree 里改，改完需合并回主目录 Chrome 才加载得到。写入 `{"worktree": {"bgIsolation": "none"}}` 可关闭（CLAUDE.md「开发与验证」已写明两种情况的做法）。
- [ ] 深色模式（暂缓）。
- [ ] 最新一版 popup（折叠 / 单条删除 / 清空确认）尚未在真实 Chrome 中确认。
- 已知限制（暂不处理）：
  - hostname 精确匹配，`www.example.com` 与 `example.com` 视为不同网站。
  - 恢复后的书签 `dateAdded` 变为恢复时刻；`trash` 仅存本机，不随 Chrome 同步。
  - 并发写 `trash` 的测试只证明加锁后结果正确，未验证去掉锁时会失败。
- 已决定不做：按「书」（URL 路径前缀）细化粒度——各站 URL 结构不同，需要逐站配置规则，属于产品层面的大改动。

## 开发记录

### 2026-09-26 完善 README

- **改动**：README 从只有标题补全为简介、popup 截图、功能、安装（Release zip 或 clone）、使用、注意事项（删除不可撤销、同步、hostname 精确匹配与同域多本书互相替换）、权限说明、开发入口。
- 截图 `images/screenshot-popup.png` 由本地预览（模拟 `chrome` API + headless Chrome）生成，使用虚构数据。
- 权限说明中「不读取网页内容、不发送网络请求」已核对：代码中无 `fetch` / `XMLHttpRequest` / content script。

### 2026-09-26 最近删除：折叠、删除单条、清空、7 天过期、删除时间（`9b370fc`）

- **背景**：「最近删除」一直显示、无法删除或清空；打开 popup 就能看到删过的书签标题，有隐私问题。
- **决策**：五项全做（删除单条、清空全部、自动过期、默认折叠、显示删除时间），过期时间 7 天。
- **改动**：
  - `trash` 读取时过滤超过 7 天的记录，下次写入时从存储清除。
  - **`trash` 的写操作统一在 service worker 内经 `with_trash_lock` 串行执行**，popup 只读并通过 `discard` / `clear` 消息请求。原因：popup 与 service worker 并发「读-改-写」会互相覆盖、丢记录。
  - popup：默认折叠；每条显示「刚刚 / N 分钟前 / N 天前」；「×」删除单条（不可恢复）；「清空」点一次变红色「确认清空？」，3 秒内再点才执行。
- **验证**：模拟测试新增 5 项（删除单条、清空、过期读取隐藏、过期写入清除、并发不丢记录），共 22 项通过；截图检查折叠、展开、悬停、确认清空、空列表。截图中发现「×」隐藏时仍占位导致按钮不对齐，改为常显浅灰。

### 2026-09-26 修复连点恢复会重复恢复书签（`b632134`）

- **问题**：连点「恢复」时，两个请求都能在 `trash` 中找到同一条记录，恢复出多个副本；且 `restoring_urls` 是 Set，同一 URL 只记一次，第二个副本可能被当成新书签，反而删掉刚恢复的书签。
- **改动**：service worker 记录正在恢复的条目 id，重复请求直接忽略（多个 popup 也能防住）；popup 点击后按钮置灰。
- **验证**：先写复现测试，修复前失败（出现两个副本），修复后通过。

### 2026-09-26 popup 界面美化（`02c87b4`）

- **背景**：功能正常，但基本是 HTML 默认样式；「恢复」按钮被挤成竖排两行。
- **改动**：重写 `hello.css`（配色变量、顶部网站信息与开关分区、更紧凑的开关、警示卡片式确认框、主次按钮）；最近删除每条改为标题 + 灰色网址两行，按钮不换行。开关 class 名保持不变，JS 逻辑未动。
- **验证**：本地预览 + headless Chrome 截图检查开启、确认、不支持页面、空列表四种状态，修正了列表缩进和「OFF」文字贴着滑块两处。
- **排查记录**：Chrome 扩展工具未连接；本地 HTTP 服务无响应，改用 `file://` + `--allow-file-access-from-files`；headless Chrome 截图后不退出，脚本改为检测到截图文件后主动结束进程。

### 2026-09-26 开启确认、最近删除与恢复、导入保护（`968409f`）

- **背景**：开启开关后，只要新建一次书签就会清掉该网站之前积累的所有收藏，且删除不可撤销，开了 Chrome 同步的话会同步到其他设备。
- **风险分析**：一个网站多本书（按 hostname 判断等于「一站一签」）；开启时已有 N 个收藏；跨文件夹误删；收藏错了没法撤回；导入书签连锁删除。
- **决策**：旧收藏选「显示数量并确认后接管」；做数量确认、最近删除与恢复、导入保护三项；不做按书细化。
- **改动**：
  - popup 显示 hostname 与本站收藏数，多于 1 个时在 popup 内确认再开启。
  - 删除前写入 `trash`，popup 列出最近删除并可恢复到原文件夹原位置（文件夹不存在则放默认位置，位置越界则修正）。
  - **恢复必须经由 service worker**：恢复本身会触发 `onCreated`，不处理的话恢复出的旧书签会被当成最新，反而删掉当前书签。service worker 先记下待恢复的 URL，对应的 `onCreated` 跳过。
  - `onImportBegan` ~ `onImportEnded` 期间不处理新书签。
  - `findBookMarksByDomain` 移至 `utils/bookmark.js` 并改名 `find_bookmarks_by_domain`（popup 统计数量也要用）。
- **验证**：模拟测试 15 项通过（跨文件夹删除、进入 trash、恢复原位且不删当前书签、文件夹缺失降级、位置越界修正、导入期间不删、导入后恢复、未开启网站不受影响、trash 上限 50）。

### 2026-09-26 开关改为按网站区分（`e604707`）

- **背景**：函数名 `curr_tab_site_is_recorded` 暗示按网站，实际开关是全局的——一旦开启，对所有网站生效，比如收藏一个 GitHub 页面会删掉其他所有 GitHub 书签。
- **决策**：选「改成按网站开关」，而不是只改名；删除旧版全局开关 key；顺手修复新建文件夹时报错。
- **改动**：
  - `utils/tab.js` 改名为 `utils/site.js`，存储改为 `enabled_sites: { hostname: 开启时间 }`。
  - popup 读取当前 tab 的 hostname，只切换该网站；非 http(s) 页面开关置灰。读取 `tab.url` 靠已有的 `host_permissions`，不需要加回 `activeTab`。
  - service worker 按**新书签自身的 hostname** 判断（书签管理器里新建时当前 tab 不一定是该网站）；书签无 `url`（文件夹）时直接跳过；`onInstalled` 时删除旧 key `recorded_datetime`。
- **验证**：模拟测试通过（旧 key 清除、开启网站只留最新、未开启网站不受影响、新建文件夹不报错）。

### 2026-09-25 CLAUDE.md、权限与删除逻辑修复（`d9064d9`、`8d60133`、`917a0fe`）

- 新建 `CLAUDE.md`：加载调试方式、架构、约定。
- 移除 `manifest.json` 中未使用的 `activeTab` 权限。
- 删除旧书签由 `forEach(async)` 改为 `for...of` + `await`，每个删除单独 `try/catch`，一个失败不影响其余。
- `.gitignore` 忽略 `.claude/`。

### 2025-10 原型（`243db5a`、`1a85c16`、`4ab0c58`）

- 同一网站只保留最新书签的原型；加全局开关；添加图标。
